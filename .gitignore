node_modules/
dist/
backend/certs/*.pem
.env
*.log

# Personal/business reference documents — not source code
เอกสาร Champ/
~$*.xlsx
*.zip
*.rar
*.lnk

# Personal photo/video sync folder that ended up under backend/public/ —
# unrelated to the landing-page car-brand image galleries (those stay).
backend/public/Pictures/

# Local DB dumps — real customer/financial data, never belongs in git
backend/backups/

# Local SQLite database files — may contain real data, never belongs in git
backend/data/*.db
backend/data/*.db-shm
backend/data/*.db-wal

# Local scratch/test image folders and a recruitment poster — not app assets,
# kept on disk but never tracked
frontend/src/image/test1/
frontend/src/image/test2/
frontend/src/image/รับสมัครงาน.png

# รูปที่ผู้ใช้อัปโหลดจริงตอนรันไทม์ (job_photos, quote_part_prices.image_data) —
# เก็บเป็นไฟล์บนดิสก์เซิร์ฟเวอร์เท่านั้น ไม่ใช่ asset ของโค้ด ไม่ควรอยู่ใน git เลย
backend/uploads/
